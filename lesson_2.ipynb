from json.tool import main
import pandas as pd
import os

def add_two_numbers(number_1: int, number_2: int) -> int:
    return number_1 + number_2

add_two_numbers(2,3) = 5 
add_two_numbers(5,3) = 5 
add_two_numbers(2, "aaaa") = 5 

def read_csv_file(csv_path: str) -> pd.DataFrame:
    df = pd.read_csv(csv_path)
    return df

def main():
    df = read_csv_file



if __name__ == "__main__":
    main()

def calculate_mad_for_col(col_nam: str) -> float:


prof_report #analizuje wszystko pod wszystkimi możliwościami statystycznymi

from pandas_profitin import ProfileReport
sns.pairplot(penguin)


sns.penguin
penguins.shape()
penguins with streamlit

species = penguins("species").unique()
change_species = st.selectbox